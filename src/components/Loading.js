import React from 'react';
import './Loading.css';

const Loading = () => {
  return (
    <div className="wrapper">
      <div className="loading loader"></div>
    </div>
  );
};

export default Loading;
